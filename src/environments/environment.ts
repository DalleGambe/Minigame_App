// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
   firebaseConfig: {
     apiKey: 'AIzaSyBefZhS372VZXqQ2Gl3ZpZ26lBIPQN-Avw',
     authDomain: 'mobiele-minigame-app.firebaseapp.com',
     projectId: 'mobiele-minigame-app',
     storageBucket: 'mobiele-minigame-app.appspot.com',
     messagingSenderId: '149067789961',
     appId: '1:149067789961:web:627730328edd81ec47b858'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
